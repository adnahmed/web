type Examinee implements User{
    id: UUID!
    first_name: String
    middle_name: String
    last_name: String
    groups: [Group]
    completedExams: [Exam!]
    completedPaper: [Paper!]
}

type Mutation {
    registerExaminee(username: String!, password: String!, first_name:String!, middle_name:String, last_name:String): UserRegisterResponse
}